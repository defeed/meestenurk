<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
	  <%= stylesheet_link_tag    "application", :media => "all" %>
	  <%= javascript_include_tag "application" %>
	  <%= csrf_meta_tags %>
		<meta name="robots" content="noindex, nofollow">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "Meestenurk", store_url, :class => "brand" %>
          <div class="nav-collapse">
            <%= render 'layouts/site_menu' %>
						<%= render 'layouts/search_form' %>
						<%= render 'layouts/language_select' %>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
			<div class="row">
				<div class="span3" id="logo">
					<%= image_tag('meestenurk-logo4.png', :width => '100%') %>
				</div>
				<div class="span9">
				<% unless "carts" == controller.controller_name %>
					<%= render :partial => 'carts/top' %>
				<% end %>
				</div>
			</div>
      <div class="row">
        <%= render 'layouts/sidebar' %>
				<%= render 'layouts/messages' %>
				<%= yield %>
      </div>

      <hr>

      <footer>
        <p>
					&copy; 2012 Meestenurk O&Uuml; <span class="pull-right"><%= link_to(content_tag(:i, "", :class => "icon-lock"), login_path, :title => "#{t '.login'}") unless current_user %></span>
				</p>
      </footer>

    </div>

  </body>
</html>