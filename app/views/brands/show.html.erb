<div class="span9">
	<h1><%= @brand.name %></h1>
	<p><%= @brand.description %></p>
	<ul class="thumbnails">
		<%= render @products %>
	</ul>
	<%= will_paginate @products, :class => "pagination pagination-centered" %>
</div><!--/span-->
